@{
    ViewBag.Title = "View QR Code";
}

<h2>Your QR Code</h2>

@if (ViewBag.QRCodeUrl != null)
{
    <img src="@ViewBag.QRCodeUrl" alt="QR Code" style="max-width: 100%; height: auto;" />
}
else
{
    <p>No QR code available.</p>
}